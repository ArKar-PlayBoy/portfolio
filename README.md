# Portfolio

Personal portfolio built with React + Vite.

## Available Scripts

### `npm start`
Runs the development server at [http://localhost:5173](http://localhost:5173).

### `npm run build`
Builds for production to the `dist` folder.

### `npm run serve`
Previews the production build locally.
